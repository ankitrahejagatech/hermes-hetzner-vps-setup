# Copy values into ~/.hermes/.env on the VPS.
# Do not commit the real production file.

TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=
MESSAGING_CWD=/home/hermes/projects

# Add your LLM/API provider values from `hermes setup`.
# Example names vary by provider; use the values Hermes creates.
